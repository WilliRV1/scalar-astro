# 14 — Auditoría del 24 de septiembre de 2026

Cuatro revisiones de solo lectura (seguridad de base de datos, operación,
frontend de administración, frontend de coach/atleta) más lo que salió del
primer box real en producción. Se lista lo que **falta**; lo que ya se arregló
va marcado con ✅ y su commit.

Severidad: **A** = pérdida de datos, plata o acceso indebido · **M** = el
usuario lo nota y se queja · **B** = pulido.

## Arreglado en la misma jornada ✅

| Qué | Commit |
|---|---|
| Volver del selector de archivos borraba el formulario (la caché se vaciaba en cada evento de sesión); el selector de box mostraba al coach y a los atletas como boxes propios | `7c4e9c3` |
| El logo va primero y queda guardado al subirlo | `d28ce36` |
| `permission denied for function org_settings_defaults` al guardar Cobros | `caae3fd` |
| `athlete_activity`, `athlete_no_shows`, `automation_settings_of`, `reservation_settings_of` devolvían datos de cualquier box a cualquier usuario | `caae3fd` |
| Un admin podía ascenderse a owner o darse permisos | `caae3fd` |
| Menú del celular; gráfica de origen recortada | `bf2a36b` |

## Seguridad y base de datos

| Sev | Dónde | Qué | Arreglo |
|---|---|---|---|
| M | `supabase/functions/wompi-webhook/index.ts:68` | Verifica la firma con un secreto de eventos **global**, pero las credenciales son **por box** (`org_setup.sql:504`). Los boxes con cuenta Wompi propia no conciliarán pagos. | Resolver el box por `reference` y verificar con su `wompi_events_secret`. |
| B | `supabase/functions/_shared/http.ts:20` | CORS cae a `*` si falta `SITIO_PERMITIDO`. | Fijar el origen antes de producción. |
| B | `core_tenancy.sql:14` | `touch_updated_at()` sin `set search_path`. | Añadirlo. |
| B | Políticas de `athlete_health` | Cualquier coach lee lesiones sin permiso extra. Confirmar contra `docs/07`. | Decidir. |

Verificado bien: aislamiento por `org_id` en todas las políticas; storage por
carpeta de box; firma del webhook antes de escribir; secretos de cron en
tiempo constante; sin secretos en logs; dinero en `bigint` centavos; notas del
coach invisibles para el atleta; invitaciones atadas al correo.

## Operación

| Sev | Qué | Arreglo |
|---|---|---|
| A | **Ninguna tarea programada corre.** Cobros, avisos, clases y riesgo son funciones listas sin `pg_cron`. | Migración con `create extension pg_cron` y `cron.schedule` para las funciones SQL (`generate_invoices`, `generate_classes`, `close_class`, `run_platform_dunning`, `refresh_risk_scores`); las que necesitan Edge Function, vía `net.http_post`. |
| A | **Sin respaldos** de la base de Scalar en widawi. `docs/11-operacion.md` describe un `db-export` que no existe. | Timer systemd diario: `pg_dump -Fc` + tar de `storage-data`, 14 días, restauración probada al mes. |
| A | `master` y `origin/master` todavía contienen `.env` (URL y anon key del Supabase `coach`), `fix_schema.sql` y `node_modules/`. El repo es público. | Confirmar que el merge los elimina; rotar la anon key; opcional `git filter-repo`. |
| M | `npm audit`: `react-router` (open redirect), `ws`, `xlsx` (prototype pollution, sin parche en npm). | `npm audit fix`; `xlsx` desde el CDN oficial de SheetJS o `exceljs`. Añadir `npm audit --omit=dev --audit-level=high` al CI. |
| M | nginx en widawi sin cabeceras de seguridad ni `limit_req` en `/auth/v1`. `vercel.json` sin CSP. | Copiar las cabeceras de `vercel.json` a nginx; `limit_req` 10/min en login y registro; CSP mínima. |
| M | Uptime Kuma con 0 monitores; 4 de 5 contenedores sin healthcheck. | 3 monitores HTTP + una notificación; `healthcheck:` en auth, rest, storage, web. |
| M | Logs de GoTrue con nombre y correo por cada login; nginx con IDs en la URL. | Aceptarlo en el inventario de tratamientos (Ley 1581); formato de log sin `$args`. |
| M | `/functions/v1` devuelve `{"error"}` pero el frontend lee `mensaje`: el usuario ve *"Edge Function returned a non-2xx status code"*. | Una línea en `nginx.conf`: `{"mensaje":"…","codigo":"sin_pasarela"}`. |
| B | `config.toml` dice Postgres 15; prueba y CI en 17. Registro de migraciones distinto entre `aplicar.sh` y `setup-demo.sh`. El entorno de widawi no está documentado en el repo. | Alinear versión; documentar `docker-compose.yml`, `aplicar.sh`, `gen-env.py` y el túnel. |

## Frontend — pérdida de datos

| Sev | Dónde | Qué | Arreglo |
|---|---|---|---|
| A | `features/athletes/AthleteForm.tsx:228-234` | Editar cualquier cosa de un atleta **borra su documento y contacto de emergencia**: las columnas no están en el tipo `Athlete` y el formulario manda `null`. | Añadir `document_id`, `emergency_contact_name/phone` al tipo y al formulario. |
| A | `AthleteForm.tsx:40-43` | La primera edición de la sesión **borra los campos personalizados** (`defs` vacío al inicializar). | No montar el formulario hasta que `defs` cargue. |
| A | `routes/athlete/TodayPage.tsx:71` | Un refetch fallido desmonta el `ResultForm` con el tiempo escrito. | `if (error && !wod)`. |
| M | `routes/admin/ExpensesPage.tsx:67`, `SuppliesPage.tsx:52` | Mismo patrón: un refetch fallido cierra el formulario de gasto/compra abierto. | Igual. |
| M | `features/superadmin/SoporteDrawer.tsx:77-80` | Cerrar con X/Escape deja la sesión de soporte abierta una hora. | Llamar `terminar()` en `onClose`. |
| M | `routes/coach/SchedulePage.tsx:200-273` | El drawer de clase usa una copia vieja: tras cerrar o ajustar cupo, los botones no reflejan el estado. | Guardar solo el id y buscar la clase en cada render. |

## Frontend — errores que no se ven

| Sev | Dónde | Qué |
|---|---|---|
| A | `routes/admin/AutomationsPage.tsx:71-89,219` | El interruptor de simulación y el de cada regla fallan sin aviso; si falla la carga, muestra "simulación ENCENDIDA" aunque no lo esté. |
| A | `ExpensesPage.tsx:115,187`; `SuppliesPage.tsx:174` | "Marcar pagado" y "Borrar" sin confirmación y sin mostrar error. Borrar está a 12 px de Editar. |
| A | `routes/coach/AthleteDetailPage.tsx:22-31` | Si falla la carga muestra "Sin cobros todavía": el coach cree que el atleta está al día. |
| A | `features/auth/guards.tsx:29-35` | Sin red, la consulta de membresías falla y manda a "Tu cuenta no está vinculada a ningún box" con un `TypeError` en inglés. |
| M | `PaymentMethodPage.tsx:34-85` | Si falla `useMiDebito`, ofrece "Activar" a quien ya tiene débito. |
| M | `AttendancePage.tsx:29`, `SchedulePage.tsx:265`, `PlansPage.tsx:22`, `FieldsPage.tsx:42`, `AdminHome.tsx:44`, `ReportsPage.tsx:35`, `PersonalRecords.tsx:20` | Errores de carga ignorados → vacíos falsos ("no hay planes", "$0 recaudado"). |
| M | `features/automations/mutations.ts:92` | "No mandarlo" sobre un mensaje ya enviado actualiza 0 filas y calla. |
| M | `features/finance/ReceiptLink.tsx:24` | `window.open` tras un `await`: iOS lo bloquea. |
| M | `features/athletes/AthleteForm.tsx:207`, `SchedulePage.tsx:233` | Retirar atleta y quitar plantilla fallan en silencio. |

## Frontend — celular

| Sev | Dónde | Qué |
|---|---|---|
| A | `shared/ui/Field.tsx:4` y textareas | Inputs a 14 px: **iOS hace zoom en cada campo** de toda la app. `text-base` en móvil. |
| A | `features/billing/PaymentForm.tsx:120` | `capture="environment"` abre solo la cámara: no se puede adjuntar el pantallazo de Nequi. |
| M | ~30 botones `text-xs` sin altura (`AdminHome`, `Automations`, `Expenses`, `Fields`, `Plans`, `Supplies`, `Team`, `Athletes`, `Risk`, `AthleteDetail`) | Menos de 44 px de alto. `min-h-11`. |
| M | `ExpenseForm.tsx:155`, `SupplyForms.tsx:236` | "Hasta 5 MB" no se valida: una foto de 12 MB falla con error en inglés. |
| M | `routes/coach/SchedulePage.tsx:291`; `BookingPage.tsx:217` | "Cerrar clase" (marca no-show a todos) y cancelar reserva tardía sin confirmar. |
| B | `NuevoBoxDrawer.tsx:222`, `RecordForm.tsx:107` | Sin `inputMode` para teléfono y marcas. |
| B | `shared/ui/Drawer.tsx:52` | Sin `safe-area-inset-bottom`: Guardar pegado a la barra del iPhone. |

## Frontend — textos y otros

| Sev | Dónde | Qué |
|---|---|---|
| M | `features/automations/plantillas.ts:44`; `Landing.tsx:300` | "CrossFit" en la vista previa de mensajes y en el pie. Prohibido por marca (`docs/08`). |
| M | Todos los `err.message` y `String(error)` | Llegan tal cual `duplicate key…`, `violates row-level security…`, `Failed to fetch`. Un `mensajeAmigable(err)` central. |
| M | `features/import/importar.ts:181-201` | Si la red falla después de que Postgres confirmó el lote, el reintento fila a fila **duplica hasta 50 atletas** (no hay índice único por teléfono). |
| M | `importar.ts:203-346` | Si se cierra el navegador a mitad, los atletas quedan sin suscripción ni marcas y al resubir se omiten enteros. |
| M | `features/finance/queries.ts:27` | Gastos recurrentes no se pueden editar ni borrar desde la app. |
| M | `billing/queries.ts:27,80`; `finance/queries.ts:31` | Totales de cartera, recaudo y gastos se suman en el cliente con `limit`: en un box grande salen bajos sin aviso. |
| B | `public/sw.js:92` | Cachea `index.html` sin comprobar `ok`: una página 502 puede quedar guardada. |
| B | Fechas ISO crudas en `AdminHome`, `Expenses`, `Supplies`, `AthleteDetail`, `PersonalRecords`, incluso en el mensaje de WhatsApp. | `Intl.DateTimeFormat('es-CO')`. |
| B | Etiquetas faltantes (`ExpenseForm.tsx:109`, búsqueda de bitácora, mes de gastos); `Editar/Borrar` sin `aria-label`; `ErrorNote` sin `role="alert"`. | |

## Orden sugerido

1. **Pérdida de datos** (A): `AthleteForm` (documento, emergencia, campos propios), `TodayPage`, borrados sin confirmar, simulación sin aviso.
2. **Operación** (A): `pg_cron`, respaldos, limpiar `master`.
3. **Celular** (A): inputs a 16 px, `capture` del comprobante, guardas sin red.
4. **Vacíos falsos y errores mudos** (M): un helper de error y revisar cada `EmptyState`.
5. Lo demás por pantalla, cuando se toque cada una.
